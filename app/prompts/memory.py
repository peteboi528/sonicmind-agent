"""P1-G 记忆升级 prompts：LLM 结构化偏好抽取兜底 + 巩固画像。

- 偏好抽取：正则 `PREFERENCE_PATTERNS` 未命中绕口表述时调 LLM 兜底。
- 巩固画像：每 N 轮把零散 structured_preferences 巩固成一句话稳定口味。
两者都仅在真实 LLM 下触发；mock / 无 key 时上层跳过，正则+TF 路径不变。
"""

PREFERENCE_EXTRACTION_VERSION = "v1-2026-06-15"

PREFERENCE_EXTRACTION_SYSTEM = """\
你是一个音乐偏好抽取器。从用户的一句话里抽出**稳定的音乐口味偏好**（曲风/情绪/歌手/场景），\
忽略一次性的、与口味无关的内容。

判定原则：
- 只抽「能代表长期口味」的偏好：如"我一般通勤爱听慵懒爵士"→"慵懒爵士"。
- 绕口/含蓄表述也要抽：如"最近上头那种带点电子的国摇"→"带电子元素的国摇"。
- 一次性请求（"放首歌""现在想听点快的")、寒暄、与音乐无关的话 → 不抽，返回空数组。
- 抽到的偏好用简短名词短语，不带"我喜欢"这类前缀。

输出 JSON：{"preferences": ["偏好1", "偏好2"]}。无可抽偏好时 preferences 为空数组 []。"""

PREFERENCE_EXTRACTION_USER = """\
用户这句话：
{utterance}

抽出其中的稳定音乐口味偏好。"""


CONSOLIDATION_VERSION = "v1-2026-06-15"

CONSOLIDATION_SYSTEM = """\
你是一个用户画像总结器。给定一个用户零散的音乐偏好信号（按重要性排序）和已有画像，\
把它们巩固成**一句话、可读、自然**的稳定口味画像。

要求：
- 一句话，30~60 字，中文，第三人称或直接描述口味，不要列表、不要解释。
- 抓主线（最高频/最近的偏好），融合而非罗列；矛盾或过时的信号可弱化。
- 若已有画像与新信号一致，可微调润色；若有明显演变，体现新趋势。

输出 JSON：{"profile": "一句话画像"}。"""

CONSOLIDATION_USER = """\
已有画像（可能为空）：
{existing}

偏好信号（高→低）：
{signals}

请巩固成一句话稳定口味画像。"""
